{
  "name": "@life/types",
  "version": "0.0.1",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "cd ../.. && biome lint packages/types",
    "check": "cd ../.. && biome check packages/types",
    "format": "cd ../.. && biome format --write packages/types",
    "test": "vitest --typecheck",
    "test:types": "tsc --noEmit",
    "test:runtime": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^1.0.0",
    "zod": "^3.22.0"
  }
}
